/**
 * DisclosurePolicy Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: port-parties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { DisclosurePolicyEntity } from "@phytoseal/core/port-parties/models/index.js";
import { portPartiesSchemas } from "@phytoseal/core/port-parties";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "pty";

export function createDisclosurePolicy(overrides: Partial<DisclosurePolicyEntity> = {}) {
  const raw = {
    id: overrides.id ?? `dpl_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    documentClass: overrides.documentClass ?? faker.lorem.word(),
    name: overrides.name ?? faker.person.fullName(),
    rules: overrides.rules ?? [],
    status: overrides.status ?? "draft",
    ...overrides,
  };

  return portPartiesSchemas.DisclosurePolicy.parse(raw);
}

export function disclosurePolicyBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createDisclosurePolicy(overrides));
}
