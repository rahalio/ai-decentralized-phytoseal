/**
 * GetCertificate handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: certificates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCertificate } from "@phytoseal/api-server/domains/certificates/handlers";
import type { CertificatesDDDDependencies } from "@phytoseal/api-server/domains/certificates";
import { createCertificate as createCertificateEntity } from "@phytoseal/factories/certificates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@phytoseal/tests/shared";
import { CERTIFICATES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCertificate", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CertificatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CertificatesDDDDependencies>(
      CERTIFICATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createCertificateEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCertificate, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { certificateId: entity.id };

    await getCertificate(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCertificate.execute).toHaveBeenCalledWith(expect.objectContaining({ certificateId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { certificateId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetCertificate, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getCertificate(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
