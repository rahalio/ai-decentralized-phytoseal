/**
 * Amendment Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: amendments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { AmendmentEntity } from "@phytoseal/core/amendments/models/index.js";
import { amendmentsSchemas } from "@phytoseal/core/amendments";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "amd";

export function createAmendment(overrides: Partial<AmendmentEntity> = {}) {
  const raw = {
    id: overrides.id ?? `amd_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    certificateId: overrides.certificateId ?? `crt_${ulid().toLowerCase()}`,
    newPayloadHash: overrides.newPayloadHash ?? faker.lorem.word(),
    priorPayloadHash: overrides.priorPayloadHash ?? faker.lorem.word(),
    rationale: overrides.rationale ?? faker.lorem.word(),
    status: overrides.status ?? "proposed",
    ...overrides,
  };

  return amendmentsSchemas.Amendment.parse(raw);
}

export function amendmentBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createAmendment(overrides));
}
