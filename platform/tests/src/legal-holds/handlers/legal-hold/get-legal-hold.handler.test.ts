/**
 * GetLegalHold handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: legal-holds
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getLegalHold } from "@phytoseal/api-server/domains/legal-holds/handlers";
import type { LegalHoldsDDDDependencies } from "@phytoseal/api-server/domains/legal-holds";
import { createLegalHold as createLegalHoldEntity } from "@phytoseal/factories/legal-holds";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@phytoseal/tests/shared";
import { LEGALHOLDS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetLegalHold", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LegalHoldsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LegalHoldsDDDDependencies>(
      LEGALHOLDS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createLegalHoldEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetLegalHold, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { holdId: entity.id };

    await getLegalHold(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetLegalHold.execute).toHaveBeenCalledWith(expect.objectContaining({ holdId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { holdId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetLegalHold, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getLegalHold(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
