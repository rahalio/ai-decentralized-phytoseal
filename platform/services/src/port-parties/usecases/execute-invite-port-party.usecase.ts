/**
 * ExecuteInvitePortParty - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: port-parties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteInvitePortParty Use Case
 *
 * DDD: Application use case for create Invite.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { InvitePortPartyInput, InvitePortPartyOutput } from "../dto/invite.dto";
import type { ExecutionContextService, IdGeneratorService } from "@phytoseal/services/_shared/index.js";
import type { InviteRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteInvitePortParty {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly invite: InviteRepository
  ) {}

  async execute(input: InvitePortPartyInput): Promise<InvitePortPartyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ptyId();

    // All field names extracted from OpenAPI spec: contactEmail, message
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.contactEmail) {
      throw new ValidationError("contactEmail is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (port-parties domain)
    const entityId = this.idGenerator.ptyId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.invite.invitePortParty({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as InvitePortPartyOutput;
  }
}
