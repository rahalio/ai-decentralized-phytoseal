/**
 * PortParties DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: port-parties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * PortParties Domain Module - Composition root
 *
 * DDD: Composition root for port-parties domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  DisclosurePolicyRepositoryAdapter,
  InviteRepositoryAdapter,
  PortPartyRepositoryAdapter,
  SimulateRepositoryAdapter,
} from "@phytoseal/adapters/port-parties";
import { getIdGeneratorService } from "@phytoseal/adapters";
import type { AdapterDynamoDBClient } from "@phytoseal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateDisclosurePolicy,
  ExecuteCreatePortParty,
  ExecuteGetDisclosurePolicy,
  ExecuteGetPortParty,
  ExecuteInvitePortParty,
  ExecuteListDisclosurePolicies,
  ExecuteListPortParties,
  ExecuteReplaceDisclosurePolicy,
  ExecuteSimulateDisclosurePolicy,
  ExecuteUpdatePortParty,
} from "@phytoseal/services/port-parties/usecases";
// Ports (interfaces)
import type {
  DisclosurePolicyRepository,
  InviteRepository,
  PortPartyRepository,
  SimulateRepository,
} from "@phytoseal/services/port-parties/ports";

export interface PortPartiesDomainModule {
  repos: {
    disclosurePolicies: DisclosurePolicyRepository;
    invites: InviteRepository;
    portParties: PortPartyRepository;
    simulates: SimulateRepository;
  };
  useCases: {
    disclosurePolicies: {
      create: ExecuteCreateDisclosurePolicy;
      get: ExecuteReplaceDisclosurePolicy;
      list: ExecuteListDisclosurePolicies;
    };
    invites: {
      create: ExecuteInvitePortParty;
    };
    portParties: {
      create: ExecuteCreatePortParty;
      get: ExecuteGetPortParty;
      list: ExecuteListPortParties;
      update: ExecuteUpdatePortParty;
    };
    simulates: {
      get: ExecuteSimulateDisclosurePolicy;
    };
  };
}

export function buildPortPartiesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): PortPartiesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      disclosurePolicies?: DisclosurePolicyRepository;
      invites?: InviteRepository;
      portParties?: PortPartyRepository;
      simulates?: SimulateRepository;
    } = {};

    obj.disclosurePolicies = new DisclosurePolicyRepositoryAdapter(dynamoClient);
    obj.invites = new InviteRepositoryAdapter(dynamoClient);
    obj.portParties = new PortPartyRepositoryAdapter(dynamoClient);
    obj.simulates = new SimulateRepositoryAdapter(dynamoClient);
    return {
      disclosurePolicies: obj.disclosurePolicies!,
      invites: obj.invites!,
      portParties: obj.portParties!,
      simulates: obj.simulates!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    disclosurePolicies: {
      create: new ExecuteCreateDisclosurePolicy(executionContext, idGenerator, repos.disclosurePolicies),
      get: new ExecuteReplaceDisclosurePolicy(executionContext, idGenerator, repos.disclosurePolicies),
      list: new ExecuteListDisclosurePolicies(executionContext, idGenerator, repos.disclosurePolicies),
    },
    invites: {
      create: new ExecuteInvitePortParty(executionContext, idGenerator, repos.invites),
    },
    portParties: {
      create: new ExecuteCreatePortParty(executionContext, idGenerator, repos.portParties),
      get: new ExecuteGetPortParty(executionContext, idGenerator, repos.portParties),
      list: new ExecuteListPortParties(executionContext, idGenerator, repos.portParties),
      update: new ExecuteUpdatePortParty(executionContext, idGenerator, repos.portParties),
    },
    simulates: {
      get: new ExecuteSimulateDisclosurePolicy(executionContext, idGenerator, repos.simulates),
    },
  };
  return { repos, useCases: useCases as PortPartiesDomainModule["useCases"] };
}
