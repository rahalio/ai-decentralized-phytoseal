/**
 * Classifications DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: classifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Classifications Domain Module - Composition root
 *
 * DDD: Composition root for classifications domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ClassificationProposalRepositoryAdapter,
  KnowledgeAnnotationRepositoryAdapter,
  ReviewRepositoryAdapter,
} from "@phytoseal/adapters/classifications";
import { getIdGeneratorService } from "@phytoseal/adapters";
import type { AdapterDynamoDBClient } from "@phytoseal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateClassificationProposal,
  ExecuteCreateKnowledgeAnnotation,
  ExecuteGetClassificationProposal,
  ExecuteGetKnowledgeAnnotation,
  ExecuteListClassificationProposals,
  ExecuteListKnowledgeAnnotations,
  ExecuteReviewClassificationProposal,
} from "@phytoseal/services/classifications/usecases";
// Ports (interfaces)
import type {
  ClassificationProposalRepository,
  KnowledgeAnnotationRepository,
  ReviewRepository,
} from "@phytoseal/services/classifications/ports";

export interface ClassificationsDomainModule {
  repos: {
    classificationProposals: ClassificationProposalRepository;
    knowledgeAnnotations: KnowledgeAnnotationRepository;
    reviews: ReviewRepository;
  };
  useCases: {
    classificationProposals: {
      create: ExecuteCreateClassificationProposal;
      get: ExecuteGetClassificationProposal;
      list: ExecuteListClassificationProposals;
    };
    knowledgeAnnotations: {
      create: ExecuteCreateKnowledgeAnnotation;
      get: ExecuteGetKnowledgeAnnotation;
      list: ExecuteListKnowledgeAnnotations;
    };
    reviews: {
      get: ExecuteReviewClassificationProposal;
    };
  };
}

export function buildClassificationsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ClassificationsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      classificationProposals?: ClassificationProposalRepository;
      knowledgeAnnotations?: KnowledgeAnnotationRepository;
      reviews?: ReviewRepository;
    } = {};

    obj.classificationProposals = new ClassificationProposalRepositoryAdapter(dynamoClient);
    obj.knowledgeAnnotations = new KnowledgeAnnotationRepositoryAdapter(dynamoClient);
    obj.reviews = new ReviewRepositoryAdapter(dynamoClient);
    return {
      classificationProposals: obj.classificationProposals!,
      knowledgeAnnotations: obj.knowledgeAnnotations!,
      reviews: obj.reviews!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    classificationProposals: {
      create: new ExecuteCreateClassificationProposal(executionContext, idGenerator, repos.classificationProposals),
      get: new ExecuteGetClassificationProposal(executionContext, idGenerator, repos.classificationProposals),
      list: new ExecuteListClassificationProposals(executionContext, idGenerator, repos.classificationProposals),
    },
    knowledgeAnnotations: {
      create: new ExecuteCreateKnowledgeAnnotation(executionContext, idGenerator, repos.knowledgeAnnotations),
      get: new ExecuteGetKnowledgeAnnotation(executionContext, idGenerator, repos.knowledgeAnnotations),
      list: new ExecuteListKnowledgeAnnotations(executionContext, idGenerator, repos.knowledgeAnnotations),
    },
    reviews: {
      get: new ExecuteReviewClassificationProposal(executionContext, idGenerator, repos.reviews),
    },
  };
  return { repos, useCases: useCases as ClassificationsDomainModule["useCases"] };
}
